sonar.projectKey=AnnaMaPe_202301-w7chwe-anna-marrodan-back

sonar.organization=annamape

# Encoding of the source code. Default is default system encoding

#sonar.sourceEncoding=UTF-8

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.

sonar.sources=./src

# Encoding of the source code. Default is default system encoding

sonar.sourceEncoding=UTF-8

sonar.test.inclusions=src/**/*.test.ts
sonar.coverage.exclusions= src/loadEnvironment.ts, src/index.ts, src/server/startServer.ts, src/server/app.ts, src/database/connectDatabase.ts
sonar.javascript.lcov.reportPaths=coverage/lcov.info
